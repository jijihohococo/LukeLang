import std/sqlite
import std/auth
let db = dbOpen("/tmp/luke_auth_secret_bad.db")
dbExec(db, "CREATE TABLE IF NOT EXISTS profiles(user_id TEXT, ssn TEXT)")
secret signal ssn: str
watch ssn from db as "SELECT ssn FROM profiles WHERE user_id = 'x'"
print("should_not_build")

fn argusPlaceText(id: str, x: float, y: float, w: float, h: float, text: str) -> bool {
  return __argus_place_text(id, x, y, w, h, text)
}
fn argusPlaceButton(id: str, x: float, y: float, w: float, h: float, text: str) -> bool {
  return __argus_place_button(id, x, y, w, h, text)
}
fn argusPlaceImage(id: str, x: float, y: float, w: float, h: float, src: str) -> bool {
  return __argus_place_image(id, x, y, w, h, src)
}
fn argusPlaceBox(id: str, x: float, y: float, w: float, h: float) -> bool {
  return __argus_place_box(id, x, y, w, h)
}
fn argusPlaceInput(id: str, x: float, y: float, w: float, h: float, placeholder: str, inputType: float) -> bool {
  return __argus_place_input(id, x, y, w, h, placeholder, inputType)
}
fn argusPaint() -> bool {
  return __argus_paint()
}
fn argusClear() -> bool {
  return __argus_clear()
}

fn hankaBeginColumn(x: float, y: float, w: float, h: float, pad: float, gap: float) -> bool {
  return __hanka_begin_column(x, y, w, h, pad, gap)
}
fn hankaBeginRow(x: float, y: float, w: float, h: float, pad: float, gap: float) -> bool {
  return __hanka_begin_row(x, y, w, h, pad, gap)
}
fn hankaBeginStack(x: float, y: float, w: float, h: float, pad: float, gap: float) -> bool {
  return __hanka_begin_stack(x, y, w, h, pad, gap)
}
fn hankaSetAlign(align: float) -> bool {
  return __hanka_set_align(align)
}
fn hankaSetWrap(wrap: float) -> bool {
  return __hanka_set_wrap(wrap)
}
fn hankaSlotText(id: str, w: float, h: float, text: str) -> bool {
  return __hanka_slot_text(id, w, h, text)
}
fn hankaSlotButton(id: str, w: float, h: float, text: str) -> bool {
  return __hanka_slot_button(id, w, h, text)
}
fn hankaSlotImage(id: str, w: float, h: float, src: str) -> bool {
  return __hanka_slot_image(id, w, h, src)
}
fn hankaSlotInput(id: str, w: float, h: float, placeholder: str, inputType: float) -> bool {
  return __hanka_slot_input(id, w, h, placeholder, inputType)
}
fn hankaEnd() -> bool {
  return __hanka_end()
}
fn hankaLayout() -> bool {
  return __hanka_layout()
}

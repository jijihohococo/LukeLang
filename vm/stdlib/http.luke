// Luke standard library — HTTP (Build mode, native)
// IMPORT std/http
// Uses curl under the hood on host builds. Empty TEXT on WASI for now.

THIS IS FUNCTION httpGet WITH url AS TEXT GIVES BACK TEXT DO
  GIVE BACK __luke_http_get(url)
END FUNCTION
